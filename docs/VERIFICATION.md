# 第一可运行版本验证记录

验证日期：2026-07-18（Asia/Shanghai）

## 已实际验证

- 读取 LMU 安装目录 `D:\SteamLibrary\steamapps\common\Le Mans Ultimate`。
- 官方共享内存 Header 的 SHA-256 与代码固定值一致：
  - `InternalsPlugin.hpp`: `9B6EE8CF610FA5049B18DF580A9A9BC9EBB91346FC466584D576A6442ABCF68F`
  - `PluginObjects.hpp`: `F65F1D2226AF1ACB277F8337FB10D8955DB384118FC36EEF95EA446BE058E247`
  - `SharedMemoryInterface.hpp`: `194FF1AB39030BC811540931C8B9817258727252C9A4B35FA4734BBAA16D4DDC`
- x64、`#pragma pack(4)`、一字节 bool 合约及根结构大小 324,820 字节通过测试。
- 只读导入真实 LMU Spa DuckDB：4,570 个 GPS Time 样本、100 Hz、45.69 秒、最大圈内距离大于 1,400 m。
- 真实数据只形成一个不完整圈；导入器没有将其误报为完整圈，也没有生成无证据建议。
- `.apextrace` 导出及重新打开通过；包内含 manifest、JSON、CSV、Parquet、SVG 与 PNG。
- Spa 1.29 MFT 可读并识别 Studio 397；未尝试打开或解密受保护 MAS，轨迹降级到运行时重建。
- WPF 应用实际启动，自动导入真实数据并生成 10 张页面截图；视觉检查后修复标题前景色。
- 在运行中的 LMU 上以只读权限打开 `LMU_Data`，识别到 Silverstone Grand Prix Circuit - WEC、Ferrari 296 LMGT3（参赛条目 Vista AF Corse 2025 #21）、GT3、18 台车和 5,869.22 m 赛道长度；玩家索引与 scoring 的 `mIsPlayer/mControl` 交叉校验通过。
- 实测发现并修复 scoring 根偏移 4 字节错误；修复后环境温度 20.84°C、赛道温度 20.33°C、雨量和湿度均为合理值，不再读取垃圾数值。
- ApexTrace 实际完成“开始记录 → 只读快照 → 临时 NDJSON/检查点落盘 → 结束分析 → 确认放弃并清理临时数据”流程；游戏遥测暂停时会给出明确提示。
- 修复将 `SME_UPDATE_TELEMETRY` 状态标志误当递增计数器的问题；在实际驾驶中连续落盘超过 4,000 帧，最近 500 帧覆盖 9.98 秒，稳定为 LMU 官方实时 50.0 Hz。
- 连续检查最近 1,000 个真实帧间隔，全部为 20.000 ms，最大间隔 20.000 ms，短窗口缺失率为 0%；结束记录诊断会按 50 Hz 基准报告整段估算缺失率。
- 实际闭合 Ferrari 296 LMGT3 第 4 圈：6,277 帧、125.530 秒，50 Hz 期望同为 6,277 帧（覆盖率 100%），无失效圈或坏位置样本；LMU 官方 `mLastLapTime` 为 125.566 秒，相差 36 ms。
- 实际驾驶样本验证了速度、RPM、油门、刹车、方向和车轮转速通道持续变化；车型改为读取官方 `mVehicleModel`，正确显示 Ferrari 296 LMGT3，而不是把 Vista AF Corse 参赛条目误作车型。
- ApexTrace 固定显示在 `\\.\DISPLAY1` 并以不激活方式启动，避免覆盖或抢占运行 LMU 的 2 号显示器。
- 实时收尾现在保守地将首尾片段标为不完整，只将被前后圈号包围的有效中间圈用于预测和建议。
- 修复 LMU 会话时间归零后同圈号片段被错误拼接的问题；分圈会按时间归零强制切段。首页可恢复中断的 NDJSON 临时记录，并安全跳过崩溃留下的不完整末行。
- 实际恢复本轮 3 个完整有效圈（125.530、124.600、126.650 秒），生成下一有效圈中位数基准预测 125.530 秒，并显示 88% 置信度、2.050 秒跨度、适用条件和验证方法。
- 使用保存的 Silverstone/Ferrari 会话回归 5 个完整有效圈：双圈页自动选择圈 14 对比本次最佳圈 11，总差 +0.350 秒，S1/S2/S3 分段差之和与总差一致。
- 同一真实会话自动识别 8 个转向区间，并显示距离、进弯/弯心/出弯速度、刹车点和相对最佳圈时间。
- 修复跨圈按距离取首点造成的轨迹硬折线；现在先选择一个连续完整圈，再在该圈内按 2 m 分箱，实时页也只绘制当前圈。
- 调教页从真实共享内存记录恢复 ABS 1、TC 1、TC Slip 7、TC Cut 6、前/后防倾杆 6/3，并基于 5 圈 12.0% 的重刹 ABS 介入率生成相邻一级 A/B 验证建议。
- 设置页“数据与存储”和“关于”已完成交互与截图验证；本机记录库显示 1 个包、35.8 MB，并可直接打开目录。
- 恢复后重新开始实时记录；最新短窗口 1,122 帧实测为 50.00 Hz，最大间隔 29.859 ms、无超过 30 ms 的间隔，记录文件持续增长。
- 24 项自动化测试通过，覆盖分圈、中断恢复、三圈预测、双圈分段、弯道识别、连续走线重建、存储和 UI 合约。另一个依赖实时游戏控制权的共享内存断言在 LMU 当前不由本地玩家控制时会失败，属于外部运行状态检查。
- Release 自包含 win-x64 发布成功，并从发布目录启动后再次生成 10 张 UI 截图。

## 本轮不能验证

- 未执行 60 分钟耐久、100%/125%/150% DPI 全矩阵、干净 Windows 机器和安装/卸载测试。

这些项目保持未完成状态，不作为第一可运行版本的已完成功能报告。

## 已知构建警告

`SkiaSharp.Views.WPF 3.119.4` 的 NuGet 元数据仍以传统 .NET Framework WPF 资产为兼容目标，因此 .NET 10 还原会报告 `NU1701`（并涉及其 OpenTK WPF 传递依赖）。本轮没有隐藏该警告；Debug、Release 自包含启动和全部截图均已验证画布可正常运行。
